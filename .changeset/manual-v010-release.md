---
---

Prepare the manual v0.1.0 publish for the public Boltwall packages:
`@boltwall/l402`, `@boltwall/adapters`, `@boltwall/middleware`, and
`@boltwall/proxy`.

This changeset is intentionally empty because the initial npm publish is manual:
the package manifests are set directly to `0.1.0`, and Changesets automation is
enabled only after the packages exist on npm.

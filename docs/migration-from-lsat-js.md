# Migration from lsat-js

Moving from `Tierion/lsat-js`? This table maps each legacy API to its
`@boltwall/l402` equivalent. The primary migration path is the object-first
`L402` workflow, with L402-native names and `Uint8Array`/`bigint` types.

`@boltwall/l402` exposes `L402` and `Caveat` as the main migration entry points.
Low-level helpers such as `parseAuthenticateHeader`, `parseAuthorizationHeader`,
`buildAuthenticateHeaders`, `buildAuthorizationHeader`, `mintMacaroon`,
`verifyMacaroon`, `decodeIdentifier`, `decodeBolt11Invoice`, the caveat builders,
and the satisfiers remain available for conformance tests and advanced callers.
Rows marked **Use replacement**, **Not exposed**, or **Internal only** are not
part of the public object workflow; use the Replacement column instead.

## Decision Classes

| Decision                 | Meaning                                                                                                                                                 |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `preserve-exact`         | Keep the same public name and compatible call shape because it remains correct.                                                                         |
| `preserve-compatible`    | Keep equivalent functionality, usually with L402-native names or safer types.                                                                           |
| `replace-with-migration` | Provide a documented replacement because the old API is LSAT-specific, Buffer/Node-only, number-amount based, or conflicts with current L402 semantics. |
| `drop`                   | Do not preserve. The API is not meaningful for current L402 or exposes implementation details.                                                          |

## Cross-Cutting Migration Rules

| Legacy behavior                                               | v1 decision                                                                                                                                                                                                                                                        |
| ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `LSAT` is the only scheme emitted by class serializers.       | New APIs emit `L402` by default. Legacy `LSAT` emission is explicit via `{ legacy: true }`. L402 protocol-specification.md section 10 requires accepting both schemes and recommends dual challenge emission for servers.                                          |
| `Buffer` appears in public types such as `IdentifierOptions`. | Public APIs use `Uint8Array` and `string`. Buffer-only call shapes are `replace-with-migration`.                                                                                                                                                                   |
| Amount fields are `number` sats.                              | Public amount fields use `bigint` millisatoshis per `docs/numeric-strategy.md`. Legacy numeric sats fields are migration/display compatibility only.                                                                                                               |
| Preimage verification compares hex strings directly.          | Verification uses normalized bytes and constant-time comparison on payment hashes. L402 protocol-specification.md section 6 and macaroon-spec.md Verification require `sha256(preimage) == payment_hash`; constant-time checks are required on verification paths. |
| Unknown caveats without satisfiers are ignored.               | Preserve this behavior. L402 macaroon-spec.md Verification says unknown caveats MUST be skipped, not rejected.                                                                                                                                                     |

## Public API Inventory

| Legacy API                                                   | Source                       | Decision                 | Replacement / v1 target                                                                                          | Implementation status                  | Notes and required tests                                                                                                                                                                                                                                                                      |
| ------------------------------------------------------------ | ---------------------------- | ------------------------ | ---------------------------------------------------------------------------------------------------------------- | -------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Lsat` class                                                 | `src/lsat.ts`                | `preserve-compatible`    | `L402` class                                                                                                     | Implemented                            | Preserve useful object workflow, but use L402 naming, no `Buffer`, and default modern scheme emission. Tests: LSAT and L402 token/challenge round-trips.                                                                                                                                      |
| `Lsat.type = "LSAT"`                                         | `src/lsat.ts`                | `replace-with-migration` | serializer options such as `{ legacy: true }`                                                                    | Use replacement                        | A fixed LSAT-only type conflicts with L402-native default emission. Choose LSAT output only at serialization boundaries.                                                                                                                                                                      |
| `new Lsat(options)`                                          | `src/lsat.ts`, `LsatOptions` | `preserve-compatible`    | `new L402(options)` or static constructors                                                                       | Implemented                            | Constructor options must avoid `Buffer` and represent amounts as `bigint amountMsat`.                                                                                                                                                                                                         |
| `Lsat.fromHeader(header)`                                    | `src/lsat.ts`                | `preserve-compatible`    | `L402.fromHeader(header)`                                                                                        | Implemented                            | Accepts `LSAT`, `L402`, repeated, and folded challenges. Identical dual challenges collapse to one logical object; conflicting challenges throw `ambiguous-challenge`.                                                                                                                        |
| `Lsat.fromChallenge(challenge)`                              | `src/lsat.ts`                | `preserve-compatible`    | `L402.fromChallenge(challenge)`                                                                                  | Implemented                            | Accepts raw challenge fields or full `LSAT` / `L402` challenge strings.                                                                                                                                                                                                                       |
| `Lsat.fromToken(token, invoice?)`                            | `src/lsat.ts`                | `preserve-compatible`    | `L402.fromToken(token, invoice?)`                                                                                | Implemented                            | Accepts legacy `LSAT M:r`, modern `L402 M:r`, pending trailing-colon tokens, and multi-macaroon tokens.                                                                                                                                                                                       |
| `Lsat.fromMacaroon(macaroon, invoice?)`                      | `src/lsat.ts`                | `preserve-compatible`    | `L402.fromMacaroon(macaroon, invoice?)`                                                                          | Implemented                            | Decodes the macaroon identifier to attach the payment hash. Use `inspectMacaroon()` or `getCaveats()` for caveat inspection.                                                                                                                                                                  |
| `Lsat#toToken()`                                             | `src/lsat.ts`                | `preserve-compatible`    | `L402#toToken(options?: { legacy?: boolean })`                                                                   | Implemented                            | Default emits `L402`; `{ legacy: true }` emits `LSAT`. Throws `missing-preimage` for pending objects so it cannot be mistaken for a paid Authorization credential.                                                                                                                            |
| Pending token ending with `:` when no preimage exists        | `src/lsat.ts`                | `preserve-compatible`    | `L402.fromToken(...)` pending parse plus `L402#toPendingToken(...)`                                              | Implemented pending-token support      | Preserve trailing-colon parsing for migration object state only. `toPendingToken()` is explicitly named and must not be sent as an Authorization header. `parseAuthorizationHeader` and `toToken()` continue requiring a valid preimage per L402 protocol-specification.md sections 5 and 10. |
| `Lsat#toChallenge()`                                         | `src/lsat.ts`                | `preserve-compatible`    | `L402#toChallenge(options?)` or `L402#toAuthenticateHeaders(options?)`                                           | Implemented                            | `toChallenge()` emits one current `L402` challenge by default. `toAuthenticateHeaders()` emits dual LSAT-first/L402-second server challenges by default per L402 protocol-specification.md section 10.                                                                                        |
| `Lsat#setPreimage(preimage)`                                 | `src/lsat.ts`                | `preserve-compatible`    | `L402#setPreimage(preimage)`                                                                                     | Implemented                            | Validate 32-byte hex and use `verifyPreimage`. Tests: valid, wrong length, non-hex, mismatched hash.                                                                                                                                                                                          |
| `Lsat#isExpired()`                                           | `src/lsat.ts`                | `preserve-compatible`    | `L402#isExpired()`                                                                                               | Implemented                            | Checks `valid-until=<ISO-8601>` and imported LSAT-style `expiration=<unix-ms>` caveats. Prefer `valid-until` for new credentials.                                                                                                                                                             |
| `Lsat#isPending()`                                           | `src/lsat.ts`                | `preserve-exact`         | `L402#isPending()`                                                                                               | Implemented                            | Same semantics: no preimage means pending.                                                                                                                                                                                                                                                    |
| `Lsat#isSatisfied()`                                         | `src/lsat.ts`                | `preserve-compatible`    | `L402#isSatisfied()`                                                                                             | Implemented                            | Uses constant-time payment-hash comparison via `verifyPreimage`.                                                                                                                                                                                                                              |
| `Lsat#getMacaroon()`                                         | `src/lsat.ts`                | `replace-with-migration` | `L402#inspectMacaroon()` or `L402#macaroon`                                                                      | Implemented replacement                | Legacy returned `macaroon` library JSON internals. Use `inspectMacaroon()` for JSON-safe inspection or `macaroon` for the raw base64 credential.                                                                                                                                              |
| `Lsat#getExpirationFromMacaroon(macaroon?)`                  | `src/lsat.ts`                | `preserve-compatible`    | `expirationCaveat` / `expirationSatisfier` compatibility helpers from `@boltwall/l402`                           | Implemented expiration support         | Imported LSAT caveat condition is `expiration`; new L402 macaroons should prefer the standard `valid-until` caveat shape.                                                                                                                                                                     |
| `Lsat#addFirstPartyCaveat(caveat)`                           | `src/lsat.ts`                | `preserve-compatible`    | `L402#addFirstPartyCaveat(caveat)`                                                                               | Implemented                            | Accepts a `Caveat` instance or a raw encoded caveat string. Raw strings preserve migration workflows such as `l402.addFirstPartyCaveat("expiration<1577228778197")`.                                                                                                                          |
| `Lsat#getCaveats()`                                          | `src/lsat.ts`                | `preserve-compatible`    | `L402#getCaveats()` returning `Caveat[]`                                                                         | Implemented                            | Returns project `Caveat` instances; comparator caveats decode at the object layer.                                                                                                                                                                                                            |
| `Lsat#addInvoice(invoice)`                                   | `src/lsat.ts`                | `preserve-compatible`    | `L402#addInvoice(invoice)`                                                                                       | Implemented                            | Attaches the raw BOLT 11 invoice string to the object for challenge serialization. Use `decodeBolt11Invoice(invoice)` when amount or payment-hash inspection is needed.                                                                                                                       |
| `Lsat#toJSON()`                                              | `src/lsat.ts`                | `preserve-compatible`    | `L402#toJSON()`                                                                                                  | Implemented                            | Emits JSON-safe inspectable state and intentionally omits the payment preimage because it is bearer credential material.                                                                                                                                                                      |
| `Lsat#id`                                                    | `src/lsat.ts`                | `preserve-compatible`    | `decodeIdentifier(l402.macaroon).tokenId`                                                                        | Use replacement                        | No `id` accessor is exposed. Use explicit identifier decoding instead of the legacy encoded identifier string.                                                                                                                                                                                |
| `Lsat#baseMacaroon`                                          | `src/lsat.ts`                | `preserve-compatible`    | `L402#macaroon` or `L402#macaroons`                                                                              | Implemented                            | Multi-macaroon credentials require array support. Keep single accessor only as convenience.                                                                                                                                                                                                   |
| `Lsat#paymentHash`                                           | `src/lsat.ts`                | `preserve-compatible`    | `L402#paymentHash` bytes or `L402#paymentHashHex`                                                                | Implemented                            | Internals stay `Uint8Array`; string accessor is available for JSON/display.                                                                                                                                                                                                                   |
| `Lsat#paymentPreimage`                                       | `src/lsat.ts`                | `preserve-compatible`    | `L402#paymentPreimage`                                                                                           | Implemented                            | Keep bearer credential handling out of logs.                                                                                                                                                                                                                                                  |
| `Lsat#validUntil`                                            | `src/lsat.ts`                | `preserve-compatible`    | `L402#getCaveats()` or `L402#isExpired()`                                                                        | Use replacement                        | No `validUntil` accessor is exposed. Inspect caveats directly or use `isExpired()` for expiration checks.                                                                                                                                                                                     |
| `Lsat#timeCreated`                                           | `src/lsat.ts`                | `preserve-compatible`    | `L402#timeCreated`                                                                                               | Implemented                            | Milliseconds since the Unix epoch.                                                                                                                                                                                                                                                            |
| `Lsat#invoice`                                               | `src/lsat.ts`                | `preserve-exact`         | `L402#invoice`                                                                                                   | Implemented                            | Raw BOLT 11 string.                                                                                                                                                                                                                                                                           |
| `Lsat#amountPaid`                                            | `src/lsat.ts`                | `replace-with-migration` | application payment records using `bigint` msat                                                                  | Not exposed                            | Legacy `number` sats is not preserved on public protocol surfaces. `@boltwall/l402` does not track wallet payment accounting.                                                                                                                                                                 |
| `Lsat#routingFeePaid`                                        | `src/lsat.ts`                | `replace-with-migration` | application payment records using `bigint` msat                                                                  | Not exposed                            | Same numeric policy. `@boltwall/l402` does not track wallet routing fees.                                                                                                                                                                                                                     |
| `Lsat#invoiceAmount`                                         | `src/lsat.ts`                | `replace-with-migration` | `decodeBolt11Invoice(invoice).amountMsat`                                                                        | Implemented replacement                | `decodeBolt11Invoice` owns amount decode and returns millisatoshis as `bigint`.                                                                                                                                                                                                               |
| `parseChallengePart(challenge)`                              | `src/lsat.ts`                | `drop`                   | `parseAuthenticateHeader`                                                                                        | Implemented                            | Private-ish parser helper; current parser handles quoted values and dual challenges.                                                                                                                                                                                                          |
| `Identifier` class                                           | `src/identifier.ts`          | `replace-with-migration` | `decodeIdentifier(macaroon)` structured result                                                                   | Implemented replacement                | Legacy extends `bufio.Struct` and exposes `Buffer`. Preserve the data model through structured decode output, not the class.                                                                                                                                                                  |
| `Identifier#toString()`                                      | `src/identifier.ts`          | `replace-with-migration` | `mintMacaroon(...)` for new credentials; `decodeIdentifier(...)` for inspection                                  | Use replacement                        | No public identifier encoder is exposed. Credential minting owns identifier construction.                                                                                                                                                                                                     |
| `Identifier.fromString(str)`                                 | `src/identifier.ts`          | `replace-with-migration` | `decodeIdentifier` / identifier parser                                                                           | Implemented parser migration           | Avoid implicit hex/base64 ambiguity in public API.                                                                                                                                                                                                                                            |
| `Identifier#write/read`                                      | `src/identifier.ts`          | `drop`                   | Internal encoder/decoder                                                                                         | Internal only                          | Serialization internals should not be public.                                                                                                                                                                                                                                                 |
| `LATEST_VERSION`                                             | `src/identifier.ts`          | `drop`                   | none                                                                                                             | Internal only                          | Identifier version constants are not public API. Use `decodeIdentifier(...)` for supported identifier inspection.                                                                                                                                                                             |
| `TOKEN_ID_SIZE`                                              | `src/identifier.ts`          | `drop`                   | none                                                                                                             | Internal only                          | Token-id byte length is an internal macaroon identifier detail.                                                                                                                                                                                                                               |
| `ErrUnknownVersion`                                          | `src/identifier.ts`          | `replace-with-migration` | typed project error or `unsupported-identifier-version`                                                          | Implemented as thrown code             | Do not preserve legacy error class unless error model requires it.                                                                                                                                                                                                                            |
| `decodeIdentifierFromMacaroon(raw)`                          | `src/identifier.ts`          | `replace-with-migration` | `decodeIdentifier(raw)`                                                                                          | Implemented                            | Current helper returns structured fields, not raw encoded identifier string.                                                                                                                                                                                                                  |
| `IdentifierOptions`                                          | `src/types/lsat.ts`          | `replace-with-migration` | `MacaroonIdentifierV0`                                                                                           | Implemented                            | Legacy type exposes `Buffer`.                                                                                                                                                                                                                                                                 |
| `LsatOptions`                                                | `src/types/lsat.ts`          | `preserve-compatible`    | `L402Options`                                                                                                    | Implemented                            | Replace amount numbers with `bigint` msat in APIs that expose amounts, and use L402 naming.                                                                                                                                                                                                   |
| `Caveat` class                                               | `src/caveat.ts`              | `preserve-compatible`    | `Caveat` class plus helper factories                                                                             | Implemented                            | `Caveat` is a real runtime class. Standard L402 helpers emit `condition=value`; legacy `<` and `>` comparator caveats are supported at the object/macaroon layer.                                                                                                                             |
| `new Caveat({ condition, value, comp })`                     | `src/caveat.ts`              | `replace-with-migration` | `new Caveat(condition, value, comparator?)`                                                                      | Implemented replacement                | Constructor arguments are explicit TypeScript parameters rather than the old options object. Use a raw encoded caveat string when preserving exact legacy text is easier.                                                                                                                     |
| `Caveat#encode()`                                            | `src/caveat.ts`              | `preserve-compatible`    | `Caveat#encode()`                                                                                                | Implemented                            | Emits exact `condition=value`, `condition<value`, or `condition>value` text.                                                                                                                                                                                                                  |
| `Caveat.decode(c)`                                           | `src/caveat.ts`              | `preserve-compatible`    | `Caveat.decode(c)`                                                                                               | Implemented                            | Decodes `=`, `<`, and `>` comparators; values may contain later comparator characters.                                                                                                                                                                                                        |
| `CaveatOptions`                                              | `src/types/lsat.ts`          | `replace-with-migration` | `Caveat` constructor parameters                                                                                  | Implemented replacement                | Use `new Caveat(condition, value, comparator?)` or a helper such as `validUntil(...)`.                                                                                                                                                                                                        |
| `ErrInvalidCaveat`                                           | `src/caveat.ts`              | `replace-with-migration` | thrown short error codes                                                                                         | Use replacement                        | Legacy error classes are not exposed. Public errors use stable short messages unless a later error taxonomy is added.                                                                                                                                                                         |
| `hasCaveat(rawMac, caveat)`                                  | `src/caveat.ts`              | `preserve-compatible`    | `L402.fromMacaroon(rawMac).getCaveats().some(...)`                                                               | Implemented replacement                | Returns boolean through normal array inspection, avoiding the legacy mixed `string \| boolean \| Error` shape.                                                                                                                                                                                |
| `verifyCaveats(caveats, satisfiers, options)`                | `src/caveat.ts`              | `preserve-compatible`    | `verifyCaveats(caveats, satisfiers, context)`                                                                    | Implemented caveat verification        | Preserve unknown-caveat skip and satisfyPrevious/satisfyFinal semantics.                                                                                                                                                                                                                      |
| `Satisfier` interface                                        | `src/types/satisfier.ts`     | `preserve-compatible`    | `CaveatSatisfier`                                                                                                | Implemented satisfier support          | Keep condition plus final/previous checks; use typed context instead of `any`.                                                                                                                                                                                                                |
| `SatisfyPrevious`                                            | `src/types/satisfier.ts`     | `preserve-compatible`    | `satisfyPrevious(prev, curr, context)`                                                                           | Implemented satisfier support          | Required for attenuation.                                                                                                                                                                                                                                                                     |
| `SatisfyFinal`                                               | `src/types/satisfier.ts`     | `preserve-compatible`    | `satisfyFinal(caveat, context)`                                                                                  | Implemented satisfier support          | Required for request-context verification.                                                                                                                                                                                                                                                    |
| `expirationSatisfier`                                        | `src/satisfiers.ts`          | `replace-with-migration` | `expirationSatisfier` compatibility helper from `@boltwall/l402`; standard timeout/valid-until satisfier in core | Implemented expiration support         | Imported LSAT condition `expiration` differs from current standard constraint naming.                                                                                                                                                                                                         |
| `createServicesSatisfier(targetService)`                     | `src/satisfiers.ts`          | `preserve-compatible`    | `servicesSatisfier(targetService)`                                                                               | Implemented satisfier support          | Must enforce subset attenuation per macaroon-spec.md Verification.                                                                                                                                                                                                                            |
| `createCapabilitiesSatisfier(service, targetCapability)`     | `src/satisfiers.ts`          | `preserve-compatible`    | `capabilitiesSatisfier(service, targetCapability)`                                                               | Implemented satisfier support          | Must enforce subset attenuation.                                                                                                                                                                                                                                                              |
| `getCaveatsFromMacaroon(rawMac)`                             | `src/macaroon.ts`            | `preserve-compatible`    | `L402.fromMacaroon(rawMac).getCaveats()`                                                                         | Implemented replacement                | Returns project `Caveat[]`; malformed caveat bytes are omitted from this convenience view and remain visible through `inspectMacaroon(...)`.                                                                                                                                                  |
| `verifyMacaroonCaveats(rawMac, secret, satisfiers, options)` | `src/macaroon.ts`            | `replace-with-migration` | `verifyMacaroon({ macaroons, preimage, rootKeyStore, satisfiers, context })`                                     | Implemented verifier replacement       | Current verification needs root key lookup, preimage binding, constant-time signature/hash checks, multi-macaroon support, and unknown-caveat skip.                                                                                                                                           |
| `getRawMacaroon(mac, urlSafe?)`                              | `src/macaroon.ts`            | `drop`                   | Internal `MacaroonCodec.encodeRaw`                                                                               | Implemented internal codec             | Exposes wrapped macaroon library internals and URL-safe option not used by L402 HTTP header encoding.                                                                                                                                                                                         |
| `MacaroonClass` interface                                    | `src/types/index.ts`         | `drop`                   | internal `RawMacaroon`                                                                                           | Implemented internal codec             | Do not expose wrapped library internals.                                                                                                                                                                                                                                                      |
| `Service` class                                              | `src/service.ts`             | `replace-with-migration` | plain `{ name: string; tier: number }` service objects                                                           | Implemented replacement                | Avoid `bufio.Struct`; current helpers accept plain objects.                                                                                                                                                                                                                                   |
| `ServiceClassOptions`                                        | `src/service.ts`             | `replace-with-migration` | plain `{ name: string; tier: number }`                                                                           | Use replacement                        | No exported service class options type is needed. Helper inputs use plain objects.                                                                                                                                                                                                            |
| `SERVICES_CAVEAT_CONDITION`                                  | `src/service.ts`             | `drop`                   | string literal `"services"` or `servicesCaveat(...)`                                                             | Not exposed                            | Condition constants are not part of the public API. Prefer helper factories for construction.                                                                                                                                                                                                 |
| `SERVICE_CAPABILITIES_SUFFIX`                                | `src/service.ts`             | `drop`                   | string literal `"_capabilities"` or `capabilitiesCaveat(...)`                                                    | Not exposed                            | Suffix constants are not part of the public API. Prefer helper factories for construction.                                                                                                                                                                                                    |
| `decodeServicesCaveat(s)`                                    | `src/service.ts`             | `preserve-compatible`    | `parseServicesCaveatValue(s)`                                                                                    | Implemented services parser            | Should throw typed/short errors, return array only, not `Service[] \| Error`.                                                                                                                                                                                                                 |
| `encodeServicesCaveatValue(services)`                        | `src/service.ts`             | `preserve-compatible`    | `serializeServicesCaveatValue(services)`                                                                         | Implemented services serializer        | Current `servicesCaveat` covers construction but not public decode.                                                                                                                                                                                                                           |
| `createNewCapabilitiesCaveat(serviceName, capabilities?)`    | `src/service.ts`             | `preserve-compatible`    | `capabilitiesCaveat(serviceName, capabilities)`                                                                  | Implemented                            | Current helper is L402-native and plain-data based.                                                                                                                                                                                                                                           |
| `decodeCapabilitiesValue(value)`                             | `src/service.ts`             | `preserve-compatible`    | `parseCapabilitiesCaveatValue(value)`                                                                            | Implemented capabilities parser        | Needed by built-in satisfiers.                                                                                                                                                                                                                                                                |
| `NoServicesError`                                            | `src/service.ts`             | `replace-with-migration` | thrown short error codes                                                                                         | Use replacement                        | Legacy error classes are not exposed.                                                                                                                                                                                                                                                         |
| `InvalidServicesError`                                       | `src/service.ts`             | `replace-with-migration` | thrown short error codes                                                                                         | Use replacement                        | Legacy error classes are not exposed.                                                                                                                                                                                                                                                         |
| `InvalidCapabilitiesError`                                   | `src/service.ts`             | `replace-with-migration` | thrown short error codes                                                                                         | Use replacement                        | Legacy error classes are not exposed.                                                                                                                                                                                                                                                         |
| `isHex(h)`                                                   | `src/helpers.ts`             | `drop`                   | internal hex parser                                                                                              | Implemented internally                 | Generic helper is not part of desired public surface.                                                                                                                                                                                                                                         |
| `decode(req)`                                                | `src/helpers.ts`             | `drop`                   | `decodeBolt11Invoice(invoice)`                                                                                   | Implemented invoice decode replacement | Wrapper around old `bolt11` library, including simnet patching, should not be public.                                                                                                                                                                                                         |
| `getIdFromRequest(req)`                                      | `src/helpers.ts`             | `replace-with-migration` | `decodeBolt11Invoice(invoice).paymentHash`                                                                       | Implemented invoice decode replacement | Current API should return structured invoice data.                                                                                                                                                                                                                                            |
| `stringToBytes(s)` / `utf8Encoder` / `isValue`               | `src/helpers.ts`             | `drop`                   | internal utilities if needed                                                                                     | Internal                               | Not public protocol API.                                                                                                                                                                                                                                                                      |

## Compatibility Summary

The useful legacy surface is the object workflow: parse challenge or token,
inspect macaroon/invoice/payment hash, set a preimage, serialize a paid retry,
and evaluate caveats with satisfiers. That workflow should survive under the
`L402` class facade.

The legacy implementation details should not survive: public `Buffer` types,
`bufio.Struct` inheritance, wrapped `macaroon` library JSON classes, `number`
sat amounts, mixed return types such as `string | boolean | Error`, and
LSAT-only default emission.

{
  "name": "boltwall-express-server-example",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch server.ts",
    "start": "bun run server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@boltwall/adapters": "workspace:*",
    "@boltwall/l402": "workspace:*",
    "@boltwall/middleware": "workspace:*",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "typescript": "^5.0.0"
  }
}
